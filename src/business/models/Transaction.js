function Transaction(desc, amount, date, type) {    
    this.desc = desc;
    this.amount = amount;
    this.date = date;
    this.type = type;
    this.category = category;
}

export default Transaction;